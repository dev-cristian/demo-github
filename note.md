crear una rama:

```git checkout -b feature/newchange```


Agregar cambios a una rama

```git add .```

Guardar cambios con un mensaje

```git commit -m "mensaje"```

Subir los cambios a github

```git push origin feature/newchange```